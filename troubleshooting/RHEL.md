# Troubleshooting RHEL

Verifique `PATH` con `/usr/local/bin`, `firewalld`, `grubby` y `chronyd`. Si el repositorio de Instana está deshabilitado después de instalar `stanctl`, es intencional para no persistir la Download Key en texto plano; rerun de `04-install-stanctl.sh` para actualizar.
